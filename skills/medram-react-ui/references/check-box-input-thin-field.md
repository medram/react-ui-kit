# Check Box Input Thin Field

## Install

```bash
pnpm dlx shadcn@latest add medram/react-ui-kit/check-box-input-thin-field
```

Generated source: `components/ui/check-box-input-thin-field.tsx`.

## Dependencies

- shadcn registry items: checkbox
- npm packages: None.
- runtime prerequisites: None beyond the generated item dependencies.

## Contract

- Source of truth: [`registry/source/fields/CheckBoxInputThinField.tsx`](../../../registry/source/fields/CheckBoxInputThinField.tsx).
- Exported declarations: `CheckBoxInputFieldThinProps`, `CheckBoxInputFieldThin`
- Does not own persistent application state.
- Preserve the source-defined prop, callback, loading, disabled, empty, and error behavior when composing this item.

## Framework boundary

Use the generated item in a client component when it owns events, hooks, Formik, browser APIs, or media access. The item has no Next-only import; keep Next routing/navigation in the host when needed.
