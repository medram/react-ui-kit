# Date Range Yup Schema

## Install

```bash
pnpm dlx shadcn@latest add medram/react-ui-kit/date-range-yup-schema
```

Generated source: `components/ui/date-range-yup-schema.ts`.

## Dependencies

- shadcn registry items: None.
- npm packages: yup
- runtime prerequisites: None beyond the generated item dependencies.

## Contract

- Source of truth: [`registry/source/fields/DateRangeYupSchema.ts`](../../../registry/source/fields/DateRangeYupSchema.ts).
- Exported declarations: `DateRangeYupSchema`
- Does not own persistent application state.
- Preserve the source-defined prop, callback, loading, disabled, empty, and error behavior when composing this item.

## Framework boundary

Use the generated item in a client component when it owns events, hooks, Formik, browser APIs, or media access. The item has no Next-only import; keep Next routing/navigation in the host when needed.
