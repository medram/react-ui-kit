# Use URL Hash

## Install

```bash
pnpm dlx shadcn@latest add medram/react-ui-kit/use-url-hash
```

Generated source: `hooks/use-url-hash.ts`. The hook owns browser hash synchronization and accepts an initial hash plus an optional URL-change callback. Use it only in client components.
